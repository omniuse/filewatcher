from terminal
-------------
* execute this command with default parameter: 
	java -jar "Filewatcher.jar"

* if necessary add parameters for path of file to watch and -c
	for example: java -jar "Filewatcher.jar" "tmp/foxo.txt" "-c"

